/* eslint no-return-assign: 0 */
import React from "react";
import { action, computed, useStrict } from "mobx";
import axios from "axios";

useStrict(true);

export const YourActions = (yourStore, initialState) => ({
  // actions
  // yourAction: action(obj => {
  //   yourStore.people.push(obj);
  // }),
  // computed
  // yourComputation: computed(() => {
  //   return yourStore.people.length;
  // }),
  // utilities
});
